CREATE TABLE LAND_MAFIA
(user_id INT PRIMARY KEY AUTO_INCREMENT NOT NULL,
 username varchar(100) NOT NULL,
 email varchar(100) NOT NULL,
 password varchar(25) NOT NULL,
 r_password varchar(25) NOT NULL,
 city varchar(50),
 address varchar (200)
);
